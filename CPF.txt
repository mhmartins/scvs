292 300 120 68

            //////////////////////////////////////////////////
            /*
            string Pesquizar_preco = cmbxsacolescre.SelectedValue.ToString(); //isto presenta a caixa de texto onde voce escreve o registo a ser pesquizado.

            MySqlConnection conexao = new MySqlConnection("coloca aki a tua conecao");

            MySqlCommand comandos = new MySqlCommand();

            comandos.CommandType = CommandType.Text;

             comandos.CommandText = "select preco from sacoles where idSacoles =@idSacoles";

            comandos.Connection = conexao;

            try

            {

            conexao.Open();

            comandos.Parameters.AddWithValue("@ID", textBox1.Text);

             MySqlDataAdapter adapter = new MySqlDataAdapter();

             adapter.SelectCommand = comandos;

             DataTable tabela = new DataTable();
             adapter.Fill(tabela);

            //aki voce pode comecar a ligar o teu control textbox desta forma:

            BindingSource source = new BinidngSource;

            source.DataSource = tabela;

            picturebox1.databinding.add("Image", source, "foto", true);

            textbox1.text.databinding.add("Text", source, "Nome", true);

            textbox2.databinding.add("Text", source, "Morada", true);

            bla bla bla bla bla bla bla ;;;;;continua com o resto.

            }

             catch (Exception ex)
                            {
                                ex.Message.ToString();
                            }

            finally{conexao.close();}

            }*/